import React from "react";

const Navbar = () => {
  return (
    <div>
      <div>
        <span>Testing</span>
        <div>
          <button>Register</button>
          <button>Login</button>
        </div>
      </div>
    </div>
  );
};

export default Navbar;
